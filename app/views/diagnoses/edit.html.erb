<% provide(:class_text, 'edit') %>
<% provide(:button_text, '編集') %>
<% provide(:id, @diagnosis) %>

<div class="diagnosis-forms">
  <div class="container">
    <h1>質問内容の編集</h1>
    <div class="row">
      <%= render 'form' %>
    </div>
  </div>
</div>