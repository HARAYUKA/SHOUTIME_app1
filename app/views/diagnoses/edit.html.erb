<% provide(:class_text, 'edit') %>
<% provide(:button_text, '編集') %>

<div class="container">
  <h1>質問内容の編集</h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
      <%= link_to "キャンセル", diagnosis_path(@diagnosis), class: "btn btn-dark" %>
    </div>  
  </div>
</div>